[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dream-control"
version = "0.1.0"
description = "Deterministic warden, validation codec, and scoring engine for sovereign-manifold fuzzing"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
